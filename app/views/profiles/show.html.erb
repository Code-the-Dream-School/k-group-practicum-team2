<div class="bg-gray-300 p-6 rounded-xl mb-6 pl-10">




  <div class="flex items-start gap-6">


    <div class="w-32 h-32 rounded-full overflow-hidden border bg-gray-300 flex-shrink-0">
      <% if @profile.avatar.attached? %>
        <%= image_tag @profile.avatar, class: "w-full h-full object-cover" %>
      <% else %>
        <div class="w-full h-full flex items-center justify-center text-gray-500">
          No Image
        </div>
      <% end %>
    </div>


    <div class="flex flex-col justify-center gap-4">
      <div class="flex items-center gap-6 flex-wrap">
        <h1 class="text-3xl font-bold text-black whitespace-nowrap">
          <%= @profile.first_name %> <%= @profile.last_name %>
        </h1>

        <div class="flex flex-wrap gap-3">
          <% @profile.skills.each do |skill| %>
            <span class="px-3 py-1 border border-black bg-gray-300 text-black text-sm rounded">
              <%= skill.name %>
            </span>
          <% end %>
        </div>
      </div>
      <p><%= @profile&.skill_level %></p>


      <p class="text-gray-700">
        <%= @profile.bio.presence || "No bio provided." %>
      </p>
    </div>

  </div>

  <% if current_user.profile&.id == @profile.id %>
    <div class="mt-6">
      <%= link_to "Edit Profile",
                  edit_user_profile_path(current_user, @profile),
                  class: "inline-block bg-black text-white px-4 py-2 rounded-lg hover:opacity-80 transition" %>
    </div>
  <% end %>

</div>

<h2 class="text-2xl font-bold text-black mb-6">Projects</h2>

<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">

  <% if @user_projects&.any? %>
    <% @user_projects.each do |project| %>

      <div class="border rounded-xl p-4 shadow hover:shadow-md transition">
        <h3 class="text-xl font-semibold mb-2">
          <%= project.title %>
        </h3>
        <p class="text-gray-600 mb-2">
          <%= project.description %>
        </p>
      </div>

    <% end %>

  <% else %>
    <p>No projects yet.</p>
  <% end %>

</div>
<div class="bg-gray-300 p-6 rounded-xl mb-6 mt-6">


  <h2 class="text-2xl font-bold text-black mb-6">Collaborate With Me</h2>

  <p class="text-gray-700 mb-6">
    I am looking for...
  </p>

<%# this section is yet to be implemented, placeholder for now %>
  <% if current_user.profile&.id == @profile.id %>
    <div class="mt-4">
      <%= link_to "Edit Collaboration Preferences",
                  "#",
                  class: "inline-block bg-black text-white px-4 py-2 rounded-lg hover:opacity-80 transition" %>
    </div>
  <% end %>

</div>
